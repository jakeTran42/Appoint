type Query {
  feed: [Appointment!]!
}

type Mutation {
    post(date_time: String!, service_description: String!): Appointment!
    signup(name: String!, email: String!, password: String!, phone_number: Int!): AuthPayload
    login(email: String!, password: String!): AuthPayload
}

type Appointment {
  id: ID!
  date_time: String!
  service_description: String!
  postedBy: User
}

type AuthPayload {
  token: String
  user: User
}

type User {
  id: ID!
  name: String!
  email: String!
  phone_number: Int!
  appointments: [Appointment!]!
}